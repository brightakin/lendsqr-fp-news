import authReducer, {
  authentication,
  fetchStart,
  loginUserSuccess,
  logoutUser,
} from './authSlice';

// export all Actions and Reducers
export {authReducer, authentication, fetchStart, loginUserSuccess, logoutUser};
